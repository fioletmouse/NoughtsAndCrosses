@using NoughtsAndCrosses.Models
@model List<Overall>
@{
    if(Model.Count > 0)
    {
        foreach (var summary in Model)
        {
            //привести див в порядок, а то все идентификаторы открыты
                @summary.ResultType <p> = </p>
                 @summary.Count<br />

        }
    }
    else
    {
        <p>Пусто</p>
    }
}